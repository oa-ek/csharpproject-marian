@using TeamManager.Core.Entities

@model TeamManager.Core.Entities.AdvertisementToFind

@{
    ViewData["Title"] = "Create Advertisement To Find";
    Layout = "~/Views/Shared/_Layout.cshtml";
    List<Game>? games = ViewBag.Games as List<Game>;
    List<UserGroup>? userGroups = ViewBag.UserGroups as List<UserGroup>;
}

<h2>@ViewData["Title"]</h2>

<form asp-action="Create" method="post">
    <div class="form-group">
        <label asp-for="Name"></label>
        <input asp-for="Name" class="form-control" />
        <span asp-validation-for="Name" class="text-danger"></span>
    </div>
    <div class="form-group">
        <label asp-for="Description"></label>
        <textarea asp-for="Description" class="form-control"></textarea>
        <span asp-validation-for="Description" class="text-danger"></span>
    </div>
    <div class="form-group">
        <label asp-for="Price">Price:</label>
        <div class="input-group">
            <span class="input-group-text">$</span>
            <input asp-for="Price" type="number" class="form-control" min="0" step="0.01" />
        </div>
        <span asp-validation-for="Price" class="text-danger"></span>
    </div>
    <div class="form-group">
        <label>
            <input asp-for="IsActive" type="checkbox" /> Is Active
        </label>
    </div>
    <div class="mb-3">
        <label for="games">Select Games:</label>
        <select id="games" class="form-select" name="selectedGameIds" multiple>
            @foreach (var game in games)
            {
                <option value="@game.Id">@game.Name</option>
            }
        </select>
    </div>
    <div class="mb-3">
        <label for="userGroupId">Select Group:</label>
        <select id="userGroupId" class="form-select" asp-for="userGroupId">
            <option value="">-- Select Group --</option>
            @foreach (var group in userGroups)
            {
                <option value="@group.Id">@group.Name</option>
            }
        </select>
        <span asp-validation-for="userGroupId" class="text-danger"></span>
    </div>
    <button type="submit" class="btn btn-primary">Create</button>
</form>

@section Scripts
{
    <script>
        $(function () {
            $("#games").select2();
        });

        function updatePriceValue(value) {
            document.getElementById('priceValue').textContent = value;
        }
    </script>
}
